apiVersion: 1
# Grafana 数据源自动配置：容器启动即接好 Prometheus，无需手工添加
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 15s
